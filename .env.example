# Environment Configuration for Production

# Secret key for Flask sessions (REQUIRED - generate a secure random string)
SECRET_KEY=your-super-secret-key-here

# Database URL (for production, use PostgreSQL or another cloud database)
# For Vercel, consider using Vercel Postgres, PlanetScale, or another cloud database
DATABASE_URL=postgresql://username:password@host:port/database
# Alternative variable name (for local development you can use SQLite)
SQLALCHEMY_DATABASE_URI=sqlite:///order_system.db

# Email Configuration (for order confirmations)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# Environment (set to production for Vercel)
FLASK_ENV=production

# Vercel specific
PYTHONPATH=/var/task
